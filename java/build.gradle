plugins {
    id 'java'
    id 'maven-publish'
    id 'idea'
}
version=0.0

repositories {
    jcenter()
}

dependencies {
  testCompile "junit:junit:+"
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            from components.java
        }
    }
}
